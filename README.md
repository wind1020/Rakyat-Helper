# Rakyat Helper

Malaysian civic aid advisor app. React + Vite, deployed on Cloudflare Pages.

## Local development
```
npm install
npm run dev
```

## Build
```
npm run build
```
Cloudflare Pages build settings:
- Build command: `npm run build`
- Build output directory: `dist`
